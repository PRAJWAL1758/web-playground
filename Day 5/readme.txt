
In this project, I used HTML to create the structure of the page.

I used CSS to design the layout and make it responsive.

I used JavaScript to get the current time using different time zones.

I used the Date object and setInterval to update the time every second.